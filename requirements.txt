# Core dependencies
numpy>=1.20.0
Pillow>=8.0.0
tqdm>=4.60.0
opencv-python>=4.5.0

# Dataset download dependencies
gdown>=4.6.0
huggingface-hub>=0.16.0

# ── Inference: Mac (Apple Silicon) ──────────────────────────────────────────
# pip install mlx-vlm mlx-lm

# ── Inference: Linux/GPU (HuggingFace) ──────────────────────────────────────
# PyTorch 2.7 + CUDA 12.8 (supports Flash Attention 2)
# Install via: https://pytorch.org/get-started/locally/
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
transformers>=4.49.0
accelerate>=0.26.0
peft>=0.9.0
qwen-vl-utils>=0.0.8

# Flash Attention 2 (CUDA 12.8, torch 2.7 — build from source)
# pip install flash-attn --no-build-isolation

# ── Fine-tuning: Linux/GPU ───────────────────────────────────────────────────
# Unsloth (installs its own torch/triton — follow official guide)
# pip install unsloth unsloth_zoo
# pip install trl datasets

# ── Dev / evaluation only ────────────────────────────────────────────────────
safetensors>=0.4.0
